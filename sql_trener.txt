
create table coach_players(
uid serial NOT NULL,
coachid integer references users(uid),
userid integer references users(uid),
stan integer default 0,
cdate date DEFAULT now()
);

alter table coach_players add CONSTRAINT coach_players_pkey PRIMARY KEY (uid);


insert into coach_players(coachid, userid)
select 2, uid from users where uid not in (2,3) limit 20
;

